# === Build trees / artifacts ===
build/
*.o
*.o.d
*.a
*.elf
*.elf.run
*.so
*.lo
*.obj

# === Generated gauge configs (HMC output, large) ===
sample_hmc/conf_quench_*
sample_hmc/conf_hmc_*
sample_hmc/conf_output*
**/conf_real16.txt
# gauge configs copied into test run/ dirs (regenerable, large)
# (also the -000NN thermalized snapshots, e.g. conf_quench_12x12x12x12.txt-000075)
**/run/conf*.txt*
**/nandbg/conf*.txt*

# === Editor / tool backups ===
*.orig
*.bak
*.bak_*
Makefile.20[0-9][0-9]*
*~
*.plotbak
# yaml snapshots / variant backups (main.yaml.b57bak, *.16p4, ...)
*.b57bak
*.16p4

# === WSL / Windows metadata litter ===
*:Zone.Identifier

# === LaTeX build artifacts (keep the .tex source) ===
docs/latex/*.aux
docs/latex/*.log
docs/latex/*.out
docs/latex/*.toc
docs/latex/*.pdf
docs/latex/*.synctex.gz

# === Local scratch ===
*.log.tmp
tmp_*.log
# test run binaries + run/sweep logs (regenerable)
**/run/exe
**/run/*.log
**/run/*.out
# one-off driver/rerun scratch (fp_dw_tw_driver.out, fp_dw_tw_rerun.sh/.summary)
**/run/fp_dw_tw_*
**/nandbg/*.log
run_*.log
sweep_launch.log
# parameter-sweep output trees (logs, gpu dumps, per-test confs) + prebuilt sweep binaries
**/sweep_2[0-9]*/
**/sweep_bins/
**/gpu_before
**/gpu_after

# === editor / tooling litter (keep out of source tree) ===
.progress_count
*.txtopttopt
# Claude Code local config (keep local-only)
.claude/

# microbench binaries
test_alt_amg_dw_GEMM/cinv_bench
test_alt_amg_dw_GEMM/cinv_bench8

# === run/ dirs are local scratch (canonical yamls live in test/) ===
**/run/

# === sweep / analysis scratch scripts (local-only, not for the repo) ===
**/compare_mgs_cgs2.sh
**/mg_sweep.sh

# === HPC batch / job-submission / benchmark launch scripts (local-only) ===
job_*.sh
qlogin_*.sh
acc_*.sh
cuda_*.sh
pinned_*.sh
not_pinned_*.sh
direct_cuda_*.sh
makeconfig.sh

# === large data kept on disk but out of the repo ===
# reference / built PDFs
reference/*.pdf
docs/*.pdf
# HMC test gauge-config fixtures (regenerable)
**/ConfU/*.bin
# regression baseline logs + scheduler job stdout dumps (*.sh.oNNNNN)
*.ref
*.ref[0-9]
*.sh.o[0-9]*
